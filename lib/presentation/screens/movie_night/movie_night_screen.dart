import 'package:flutter/material.dart';

class MovieNightScreen extends StatelessWidget {
  const MovieNightScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Movie Night Screen'));
  }
}
